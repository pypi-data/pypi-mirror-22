[aliases]
test = pytest

[bdist_wheel]
python-tag = py3

[tool:pytest]
addopts = --flake8 --isort --cov radicale
norecursedirs = dist .cache .git build *.egg-info .eggs venv

[egg_info]
tag_build = 
tag_date = 0

