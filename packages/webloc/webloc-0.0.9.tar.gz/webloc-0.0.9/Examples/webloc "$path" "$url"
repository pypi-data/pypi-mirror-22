#!/usr/bin/env bash
{ set +x; } 2>/dev/null

[[ $OSTYPE != *darwin* ]] && echo "SKIP: OS X only" && exit 0

path=~/.test.webloc
url="https://github.com/"
( set -x; webloc "$path" "$url" ) || exit
( set -x; webloc "$path" ) || exit
