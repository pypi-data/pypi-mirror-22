#!/usr/bin/env bash
{ set +x; } 2>/dev/null

[[ -z "$(which php)" ]] && echo "SKIP: php not installed" && exit 0

image1="${BASH_SOURCE[0]%/*}"/image1.png
image2="${BASH_SOURCE[0]%/*}"/image2.png
set -x
compare-images.php "$image1" "$image2" # $? != 0
echo exit=$?
:
