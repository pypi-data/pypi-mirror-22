#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['keyring', 'jeepney']

entry_points = \
{'keyring.backends': ['jeepney = keyring_jeepney']}

setup(name='keyring_jeepney',
      version='0.1',
      description='A pure Python keyring backend using the Freedesktop secret service.',
      author='Thomas Kluyver',
      author_email='thomas@kluyver.me.uk',
      url='https://gitlab.com/takluyver/keyring_jeepney',
      py_modules=['keyring_jeepney'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.5',
     )
