# These configurations are read by default, and are enough to configure xframes.

[spark]
spark.master=local[*]
spark.app.name=XFrames

[webhdfs]
user=hdfs
port=50070
kerberos=False

[xframes]
verbose=False
