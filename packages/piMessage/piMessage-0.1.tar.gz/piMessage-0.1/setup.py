from distutils.core import setup

setup(
    name='piMessage',
    packages=['piMessage'],  # this must be the same as the name above
    version='0.1',
    description='A library to send iMessages programmatically',
    author='Shashank Saxena',
    author_email='ssaxen4@illinois.edu',
    url='https://github.com/Salil999/piMessage',  # use the URL to the github repo
    download_url='https://github.com/Salil999/piMessage/archive/0.1.tar.gz',
    keywords=['iMessage', 'Apple', 'messaging', 'twilio', 'texting', 'text', 'phone'],  # arbitrary keywords
    classifiers=[],
)
