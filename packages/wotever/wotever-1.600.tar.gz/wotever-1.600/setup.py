#!/usr/bin/env python

from distutils.core import setup

setup(name='wotever',
      version='1.600',
      description='some useful tools',
      author='wotever',
      author_email='scott@bpp.so',
      url='http://bpp.so',
      packages=['wotever'],
     )
