from setuptools import setup

setup(
    name = 'wombat_foo',
    packages = ['wombat_foo'],
    version = '2.1234',

    author = 'wombatwen',
    author_email = 'wombatwen@gmail.com',
    scripts = ['wombat_run']
)
